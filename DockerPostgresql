FROM postgres:14

RUN apt-get update && apt-get -y install python3 postgresql-plpython3-14 && \
    apt-get clean && \
    rm -rf /var/cache/apt/* /var/lib/apt/lists/*
# ADD ./internal/sql/create.sql /docker-entrypoint-initdb.d
EXPOSE 5432
CMD ["postgres"]